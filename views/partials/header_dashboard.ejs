<header class="db-header">
  <div class="db-header-inner">

    <!-- Brand -->
    <a href="/dashboard" class="db-brand">
      <div class="db-brand-icon">
        <i class="bi bi-journal-bookmark-fill"></i>
      </div>
      <span class="db-brand-text">Notes</span>
    </a>

    <!-- Search -->
    <form class="db-search-form" role="search" method="POST" action="/dashboard/search" id="searchForm">
      <div class="db-search-wrap">
        <i class="bi bi-search db-search-icon"></i>
        <input
          type="search"
          name="searchTerm"
          id="searchTerm"
          class="db-search-input"
          placeholder="Search your notes..."
          aria-label="Search notes"
          autocomplete="off"
        >
        <kbd class="db-search-kbd">⌘K</kbd>
      </div>
    </form>

    <!-- Right actions -->
    <div class="db-header-right">
      <a href="/logout" id="logoutBtn" class="db-logout-btn">
        <i class="bi bi-box-arrow-right"></i>
        <span class="d-none d-md-inline">Logout</span>
      </a>
    </div>

  </div>
</header>

<style>
/* ─── Dashboard Header ───────────────────── */
.db-header {
  position: sticky;
  top: 0;
  z-index: 100;
  background: rgba(17, 19, 24, 0.88);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border-bottom: 1px solid rgba(255, 255, 255, 0.07);
}

.db-header-inner {
  max-width: 1400px;
  margin: 0 auto;
  padding: 0 2rem;
  height: 64px;
  display: flex;
  align-items: center;
  gap: 1.5rem;
}

/* Brand */
.db-brand {
  display: flex;
  align-items: center;
  gap: 0.6rem;
  text-decoration: none;
  flex-shrink: 0;
}

.db-brand-icon {
  width: 34px;
  height: 34px;
  border-radius: 9px;
  background: linear-gradient(135deg, #7c6ef5, #5a4fcf);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 0.95rem;
  color: #fff;
  box-shadow: 0 2px 12px rgba(124, 110, 245, 0.4);
}

.db-brand-text {
  font-family: 'Space Grotesk', sans-serif;
  font-weight: 700;
  font-size: 1.1rem;
  color: #e8eaf0;
  letter-spacing: -0.02em;
}

/* Search */
.db-search-form {
  flex: 1;
  max-width: 420px;
}

.db-search-wrap {
  position: relative;
  display: flex;
  align-items: center;
}

.db-search-icon {
  position: absolute;
  left: 1rem;
  color: #5c6070;
  font-size: 0.85rem;
  pointer-events: none;
  z-index: 1;
}

.db-search-input {
  width: 100%;
  background: #161921;
  border: 1px solid rgba(255, 255, 255, 0.07);
  border-radius: 50px;
  color: #e8eaf0;
  font-family: 'Inter', sans-serif;
  font-size: 0.875rem;
  padding: 0.55rem 2.8rem 0.55rem 2.5rem;
  transition: border-color 0.2s ease, box-shadow 0.2s ease, background 0.2s ease;
  outline: none;
}

.db-search-input::placeholder {
  color: #5c6070;
}

.db-search-input:focus {
  border-color: rgba(124, 110, 245, 0.5);
  box-shadow: 0 0 0 3px rgba(124, 110, 245, 0.12);
  background: #1a1d24;
}

.db-search-kbd {
  position: absolute;
  right: 0.9rem;
  font-size: 0.65rem;
  color: #5c6070;
  background: rgba(255,255,255,0.05);
  border: 1px solid rgba(255,255,255,0.08);
  border-radius: 4px;
  padding: 0.1rem 0.35rem;
  font-family: 'Inter', sans-serif;
  pointer-events: none;
}

/* Right side */
.db-header-right {
  margin-left: auto;
  flex-shrink: 0;
}

.db-logout-btn {
  display: inline-flex;
  align-items: center;
  gap: 0.4rem;
  color: #9095a8;
  font-size: 0.875rem;
  font-weight: 500;
  padding: 0.45rem 1rem;
  border-radius: 8px;
  border: 1px solid rgba(255,255,255,0.07);
  background: rgba(255,255,255,0.03);
  text-decoration: none;
  transition: all 0.18s ease;
}

.db-logout-btn:hover {
  color: #e8eaf0;
  border-color: rgba(240,82,82,0.3);
  background: rgba(240,82,82,0.08);
}

@media (max-width: 768px) {
  .db-header-inner { padding: 0 1rem; gap: 0.75rem; }
  .db-search-form { max-width: none; }
  .db-search-kbd { display: none; }
}
</style>